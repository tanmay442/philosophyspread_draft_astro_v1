---
interface Step {
  line: string;
  formula: string;
  justification: string;
}

interface Props {
  steps: Step[];
}

const { steps } = Astro.props;
---

<div class="my-8 overflow-x-auto bg-white border-2 border-outline p-6">
  <table class="w-full text-left">
    <thead class="bg-surface-container">
      <tr class="border-b-2 border-outline">
        <th class="p-3 font-label text-xs uppercase tracking-widest text-primary w-16">Line</th>
        <th class="p-3 font-label text-xs uppercase tracking-widest text-primary">Statement</th>
        <th class="p-3 font-label text-xs uppercase tracking-widest text-primary">Justification</th>
      </tr>
    </thead>
    <tbody>
      {steps.map((step, index) => (
        <tr class={`border-b border-outline/30 ${index === steps.length - 1 ? 'bg-surface-container-low' : ''}`}>
          <td class="p-3 font-label text-sm text-secondary">{step.line}</td>
          <td class="p-3 font-mono text-lg text-on-surface">{step.formula}</td>
          <td class="p-3 font-body text-sm text-on-surface-variant italic">{step.justification}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
